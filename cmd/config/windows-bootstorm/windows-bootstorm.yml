{{- $testNamespacesLabelKey := "kube-burner.io/test-name" -}}
{{- $testNamespacesLabelValue := .namespace -}}
{{- $namespace := .namespace -}}
{{- $sourceDVName := .sourceDVName -}}

global:
  gc: {{.GC}}
  gcMetrics: {{.GC_METRICS}}
  measurements:
    - name: dataVolumeLatency

metricsEndpoints:
- indexer:
    type: local
    metricsDirectory: ./windows-bootstorm-results
{{ if .ES_SERVER }}
- metrics: [{{.METRICS}}]
  alerts: [{{.ALERTS}}]
  indexer:
    esServers: ["{{.ES_SERVER}}"]
    insecureSkipVerify: true
    defaultIndex: {{.ES_INDEX}}
    type: opensearch
{{ end }}

jobs:
- name: create-source-dv
  jobType: create
  jobIterations: {{if .perNodeDV}}{{.workerCount}}{{else}}1{{end}}
  qps: {{.QPS}}
  burst: {{.BURST}}
  namespacedIterations: false
  namespace: {{ $namespace }}
  namespaceLabels:
    {{ $testNamespacesLabelKey }}: {{ $testNamespacesLabelValue }}
    security.openshift.io/scc.podSecurityLabelSync: false
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/audit: privileged
    pod-security.kubernetes.io/warn: privileged
  verifyObjects: true
  errorOnVerify: true
  waitWhenFinished: true
  podWait: false
  maxWaitTimeout: 1h
  jobPause: 10s
  cleanup: false
  objects:
    - objectTemplate: templates/dv.yml
      replicas: 1
      inputVars:
        dvNamePrefix: {{ $sourceDVName }}
        perNodeDV: {{.perNodeDV}}
        windowsImageURL: {{.windowsImageURL}}
        storageClassName: {{.storageClassName}}
        accessMode: {{.accessMode}}
        storageSize: {{.storageSize}}
        cdiSourceType: {{.cdiSourceType}}
        cdiSourceS3Cred: {{.cdiSourceS3Cred}}
        workerNodes: {{.workerNodes}}
      waitOptions:
        customStatusPaths:
        - key: '(.conditions.[] | select(.type == "Ready")).status'
          value: "True"

- name: create-windows-vms
  jobType: create
  jobIterations: {{.JOB_ITERATIONS}}
  qps: {{.QPS}}
  burst: {{.BURST}}
  namespacedIterations: false
  namespace: {{ $namespace }}
  namespaceLabels:
    {{ $testNamespacesLabelKey }}: {{ $testNamespacesLabelValue }}
    security.openshift.io/scc.podSecurityLabelSync: false
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/audit: privileged
    pod-security.kubernetes.io/warn: privileged
  verifyObjects: false
  errorOnVerify: false
  waitWhenFinished: false
  podWait: false
  maxWaitTimeout: 30m
  jobPause: 10s
  cleanup: false
  objects:
    - objectTemplate: templates/vm.yml
      replicas: 1
      inputVars:
        sourceNamespace: {{ $namespace }}
        sourceDVName: {{ $sourceDVName }}
        perNodeDV: {{.perNodeDV}}
        storageClassName: {{.storageClassName}}
        accessMode: {{.accessMode}}
        storageSize: {{.storageSize}}
        vmMemory: {{.vmMemory}}
        vmMemoryLimit: {{.vmMemoryLimit}}
        vmCPU: {{.vmCPU}}
        vmCPURequest: {{.vmCPURequest}}
        vmCPULimit: {{.vmCPULimit}}
        evictionStrategy: {{.evictionStrategy}}
        workerNodes: {{.workerNodes}}
        workerCount: {{.workerCount}}
        nodeDistribution: {{.nodeDistribution}}
        vmsPerNode: {{.vmsPerNode}}
