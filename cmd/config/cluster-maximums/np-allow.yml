kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: allow
spec:
  ingress:
  - ports:
    - protocol: TCP
      port: 8080
  podSelector:
    matchLabels:
      name: server-{{.Replica}}-{{.Iteration}}
  policyTypes:
  - Ingress