apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: virt-client-{{.Iteration}}-{{.Replica}}
  labels:
    kubevirt.io/os: fedora
spec:
  runStrategy: Always
  template:
    metadata:
      labels:
        kubevirt.io/os: fedora
        app: client
    spec:
      terminationGracePeriodSeconds: 0
      domain:
        resources:
          requests:
            memory: 512Mi
        devices:
          disks:
          - name: containerdisk
            disk:
              bus: virtio
          - disk:
              bus: virtio
            name: cloudinitdisk
          - name: emptydisk
            disk:
              bus: virtio
          interfaces:
          - name: primary-udn-net
            binding:
              name: {{.bindingMethod}}
      networks:
      - name: primary-udn-net
        pod: {}
      volumes:
      - name: containerdisk
        containerDisk:
          image: {{.vmImage}}
          imagePullPolicy: IfNotPresent

      - name: cloudinitdisk
        cloudInitNoCloud:
          networkData: |
            version: 2
            ethernets:
              eth0:
                dhcp4: true
          userData: |-
            #cloud-config
            password: perfscale
            chpasswd: { expire: False }
            runcmd:
              - dnf install -y --nodocs curl
              - curl -sS --fail http://udn-density-{{.Replica}}.svc.cluster.local/256.html --retry 30 --retry-delay 10
      - name: emptydisk
        emptyDisk:
          capacity: "50Mi"
