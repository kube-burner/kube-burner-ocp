---
kind: Service
apiVersion: v1
metadata:
  name: filler-svc-{{.Replica}}
spec:
  selector:
    app: filler-{{.Replica}}
    type: filler
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
