---
apiVersion: v1
kind: LimitRange
metadata:
  name: cudn-density-limits
spec:
  limits:
  - default:
      cpu: "500m"
      memory: "512Mi"
    defaultRequest:
      cpu: "10m"
      memory: "10Mi"
    type: Container
