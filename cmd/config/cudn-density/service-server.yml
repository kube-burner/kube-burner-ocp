# Per-server-deployment service: Creates server-1-svc.
# Selects the deployment's pods (2 endpoints) using the
# deployment-specific label (name=cudn-density-server-{replica}).
# Exposes ports 8080 (http) and 8443 (https).
# Creates additional OVN load balancer entries beyond the aggregate cudn-svc.
kind: Service
apiVersion: v1
metadata:
  name: server-{{.Replica}}-svc
spec:
  selector:
    name: cudn-density-server-{{.Replica}}
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
  - name: https
    protocol: TCP
    port: 8443
    targetPort: 8443
  type: ClusterIP
