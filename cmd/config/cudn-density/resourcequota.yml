---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: cudn-density-quota
spec:
  hard:
    pods: "100"
    requests.cpu: "10"
    requests.memory: "10Gi"
    limits.cpu: "20"
    limits.memory: "20Gi"
